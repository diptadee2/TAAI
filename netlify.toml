[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# `netlify dev` serves the SOURCE root (live Babel-in-browser transpile,
# same as the plain `npx serve` workflow), not the compiled dist/ build.
[dev]
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Scheduled functions — Netlify's own infrastructure invokes these on the
# cron below automatically, no separate scheduler/dependency needed. Cron
# times are UTC regardless of the function's own logic (same "no
# guaranteed local timezone" reality documented in lib/supabase.js's
# todayIST()) — 8:00 AM IST = 02:30 UTC.
[functions."discord-daily-leader"]
  schedule = "30 2 * * *"

[functions."discord-weekly-leaderboard"]
  schedule = "30 2 * * 1"

# No headers were configured at all before this, so every static asset
# (images, vendor React bundle, favicons) inherited Netlify's default
# `Cache-Control: public, max-age=0, must-revalidate` — the same policy
# meant for HTML, which forces a revalidation round-trip on every single
# load with no local disk cache, even seconds apart. That's expensive for
# a site with a real repeat-visitor base (students reloading the progress
# tracker daily) and was the biggest driver of Web bandwidth credits.
# These files aren't content-hashed, so a real content change needs a
# rename (cache-busting) to take effect immediately for cached visitors —
# acceptable here since none of these change often, and HTML pages
# themselves are deliberately NOT covered by this so they keep
# revalidating on every load as normal.
[[headers]]
  for = "/*.webp"
  [headers.values]
    Cache-Control = "public, max-age=604800, must-revalidate"

[[headers]]
  for = "/*.png"
  [headers.values]
    Cache-Control = "public, max-age=604800, must-revalidate"

[[headers]]
  for = "/favicon.ico"
  [headers.values]
    Cache-Control = "public, max-age=604800, must-revalidate"

[[headers]]
  for = "/vendor/*"
  [headers.values]
    Cache-Control = "public, max-age=604800, must-revalidate"

# ── Progress tracker (taai.live/gate-da-progress-tracker) ────────────────
# Schedule data is provided directly (as PDFs/etc.) and loaded via
# `npm run load-schedule`, not synced automatically from anywhere —
# there is deliberately no cron job or external fetch for this.
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[redirects]]
  from = "/gate-da-progress-tracker"
  to = "/gate-da-progress-tracker.html"
  status = 200

[[redirects]]
  from = "/gate-da-progress-tracker.html"
  to = "/gate-da-progress-tracker"
  status = 301

[[redirects]]
  from = "/progress"
  to = "/gate-da-progress-tracker"
  status = 301

[[redirects]]
  from = "/progress.html"
  to = "/gate-da-progress-tracker"
  status = 301

[[redirects]]
  from = "/learn"
  to = "https://taai.live/"
  status = 301
  force = true

[[redirects]]
  from = "/learn/*"
  to = "https://taai.live/"
  status = 301
  force = true

# ── New keyword-rich clean URLs (200 = serve renamed HTML file) ─────────────
[[redirects]]
  from = "/gate-da-courses"
  to = "/gate-da-courses.html"
  status = 200

[[redirects]]
  from = "/gate-da-test-series"
  to = "/gate-da-test-series.html"
  status = 200

[[redirects]]
  from = "/gate-da-toppers"
  to = "/gate-da-toppers.html"
  status = 200

[[redirects]]
  from = "/gate-da-free-notes"
  to = "/gate-da-free-notes.html"
  status = 200

[[redirects]]
  from = "/contact"
  to = "/contact.html"
  status = 200

# ── 301s from old short URLs so existing links/bookmarks don't break ────────
[[redirects]]
  from = "/courses"
  to = "/gate-da-courses"
  status = 301

[[redirects]]
  from = "/test-series"
  to = "/gate-da-test-series"
  status = 301

[[redirects]]
  from = "/testimonials"
  to = "/gate-da-toppers"
  status = 301

[[redirects]]
  from = "/resources"
  to = "/gate-da-free-notes"
  status = 301

# ── 301s from old .html extensions ─────────────────────────────────────────
[[redirects]]
  from = "/courses.html"
  to = "/gate-da-courses"
  status = 301

[[redirects]]
  from = "/test-series.html"
  to = "/gate-da-test-series"
  status = 301

[[redirects]]
  from = "/testimonials.html"
  to = "/gate-da-toppers"
  status = 301

[[redirects]]
  from = "/resources.html"
  to = "/gate-da-free-notes"
  status = 301

[[redirects]]
  from = "/gate-da-courses.html"
  to = "/gate-da-courses"
  status = 301

[[redirects]]
  from = "/gate-da-test-series.html"
  to = "/gate-da-test-series"
  status = 301

[[redirects]]
  from = "/gate-da-toppers.html"
  to = "/gate-da-toppers"
  status = 301

[[redirects]]
  from = "/gate-da-free-notes.html"
  to = "/gate-da-free-notes"
  status = 301

[[redirects]]
  from = "/contact.html"
  to = "/contact"
  status = 301

[[redirects]]
  from = "/index.html"
  to = "/"
  status = 301

# ── Blog: /blog/* → /blogs/* ────────────────────────────────────────────────
[[redirects]]
  from = "/blog"
  to = "/blogs"
  status = 301

[[redirects]]
  from = "/blog/*"
  to = "/blogs/:splat"
  status = 301
